@model BeYourMarket.Model.Models.SettingDictionary

@{
    ViewBag.Title = "Settings";
}

<div class="row">
    <div class="col-sm-12">
        <div class="panel panel-default">
            <div class="panel-heading"><h3 class="panel-title">Configure Google Analytics</h3></div>
            <div class="panel-body">
                <form action="@Url.Action("Configure", "GoogleAnalytics")" id="settingsForm" method="post" class="form-horizontal" role="form">

                    @Html.HiddenFor(x => x.ID)

                    <div class="form-group">
                        <label class="col-md-2 control-label">TrackingID</label>
                        <div class="col-md-10">
                            <input type="text" class="form-control" id="trackingID" name="trackingID" value="@Model.Value" required>
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            <button class="btn btn-primary" type="submit"><i class="fa fa-save"></i> Save</button>
                            <a href="@Url.Action("Plugins", "Plugin", new { area = "Admin" })" class="btn btn-default"><i class="fa fa-remove"></i> Cancel</a>
                        </div>
                    </div>

                </form>
            </div>

        </div>
    </div>
</div>